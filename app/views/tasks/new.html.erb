<h1>new task</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.label :details %>
  <%= f.text_area :details %>
  <%= f.check_box :completed %> Completed
  <%= f.submit %>

<%= link_to 'Back', tasks_path %>

<% end %>
